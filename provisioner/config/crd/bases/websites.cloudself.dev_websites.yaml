---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: websites.websites.cloudself.dev
spec:
  group: websites.cloudself.dev
  names:
    kind: Website
    listKind: WebsiteList
    plural: websites
    shortNames:
    - ws
    singular: website
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.websiteName
      name: Website Name
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.podIP
      name: Pod IP
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Website is the Schema for the websites API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WebsiteSpec defines the desired state of Website
            properties:
              backendId:
                description: BackendID is the reference to the MySQL database record
                minimum: 1
                type: integer
              htmlContent:
                description: HTMLContent is the HTML content to serve (max 100KB)
                maxLength: 102400
                type: string
              userId:
                description: UserID is the user who owns this website
                type: string
              websiteName:
                description: WebsiteName is the DNS-compliant name for the website
                  (lowercase, hyphens, alphanumeric)
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
            required:
            - backendId
            - htmlContent
            - userId
            - websiteName
            type: object
          status:
            description: WebsiteStatus defines the observed state of Website
            properties:
              errorMessage:
                description: ErrorMessage provides error context if provisioning failed
                type: string
              lastReconcileTime:
                description: LastReconcileTime is the timestamp of the last reconciliation
                format: date-time
                type: string
              podIpAddress:
                description: PodIPAddress is the internal IP address of the nginx
                  pod
                type: string
              status:
                description: Status represents the current lifecycle phase (pending,
                  provisioned, failed)
                enum:
                - pending
                - provisioned
                - failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
